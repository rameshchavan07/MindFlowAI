import 'package:flutter/material.dart';

class AppColors {
  // Dark backgrounds
  static const Color background = Color(0xFF0F172A);      // Deep Slate/Blue
  static const Color surface = Color(0xFF1E293B);         // Card Slate
  static const Color surfaceDark = Color(0xFF0B0F19);     // Very Dark Slate
  static const Color surfaceLight = Color(0xFF334155);    // Light Slate for borders/details

  // Neon accents
  static const Color lime = Color(0xFFA3E635);            // Electric Lime
  static const Color cyan = Color(0xFF22D3EE);            // Neon Cyan
  static const Color purple = Color(0xFF8B5CF6);          // Vibrant Purple

  // Neutral tones
  static const Color textPrimary = Color(0xFFF8FAFC);     // Off-white
  static const Color textSecondary = Color(0xFF94A3B8);   // Muted gray-blue
  static const Color textMuted = Color(0xFF64748B);       // Darker muted text
  static const Color border = Color(0xFF334155);          // Standard border

  // Glassmorphism overlays
  static const Color glassBackground = Color(0x1FFFFFFF);  // White with 12% opacity
  static const Color glassBorder = Color(0x26FFFFFF);      // White with 15% opacity
  static const Color glassShadow = Color(0x0D000000);      // Black with 5% opacity

  // Utility colors
  static const Color success = Color(0xFF10B981);         // Emerald Green
  static const Color warning = Color(0xFFF59E0B);         // Amber
  static const Color error = Color(0xFFEF4444);           // Red
}
